dependencies:
  pre:
    - gem install mdl
    - pip install codecov flake8

test:
  override:
    - flake8:
        files:
          - "**/*.py"
    - mdl:
        files:
          - "**/*.md"
  post:
    - codecov
